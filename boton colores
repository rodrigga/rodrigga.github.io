<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Tareas Aleatoria</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            position: relative; /* Necesario para posicionar elementos absolutamente */
            overflow: hidden; /* Evita que aparezcan barras de desplazamiento */
            background-color: #f4f4f4;
        }

        .container {
            text-align: center; /* Centra el contenido dentro del contenedor */
            width: 90%;
            max-width: 600px;
            background: #fff; /* Fondo blanco para el contenedor */
            padding: 20px;
            border-radius: 8px; /* Bordes redondeados */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Sombra suave */
            margin: 50px auto; /* Centra el contenedor en la página */
        }

        .welcome-text {
            font-size: 20px;
            margin-bottom: 20px; /* Espacio debajo del texto de bienvenida */
        }

        .task-item {
            position: absolute; /* Permite posicionar el elemento en coordenadas absolutas */
            padding: 10px;
            background: #fff;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            width: 200px; /* Ancho fijo para el elemento */
        }

        .task-text {
            flex: 1;
        }

        .color-1 {
            color: red;
        }

        .color-2 {
            color: blue;
        }

        .delete-button {
            background-color: #ff0000;
            color: white;
            border: none;
            padding: 5px;
            cursor: pointer;
        }

        .delete-button:hover {
            background-color: #cc0000;
        }

        .input-container {
            margin-top: 20px; /* Espacio superior para separar del texto de bienvenida */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-text">Organízate colocando tus tareas.</div>
        <div class="input-container">
            <input type="text" id="newTask" placeholder="Nueva tarea">
            <button id="addTask">Agregar Tarea</button>
        </div>
        <ul id="taskList"></ul>
    </div>
    <script src="nop.js"></script>
    
</body>
</html>




javascript




// Seleccionamos el contenedor
const container = document.querySelector('.container');

// Función para generar un color hexadecimal aleatorio
function getRandomColor() {
    return `#${Math.floor(Math.random() * 16777215).toString(16).padStart(6, '0')}`;
}




// Seleccionamos los elementos
const addTaskButton = document.getElementById('addTask');
const newTaskInput = document.getElementById('newTask');

addTaskButton.addEventListener('click', function() {
    const taskText = newTaskInput.value; // Obtener el valor del input

    if (taskText !== '') { // Si el input no está vacío
        const listItem = document.createElement('div'); // Crear un nuevo elemento <div>
        listItem.className = 'task-item'; // Añadir clase para estilos
        listItem.textContent = taskText;

        // Crear un botón de eliminar
        const deleteButton = document.createElement('button');
        deleteButton.className = 'delete-button';
        deleteButton.textContent = 'Eliminar';
        deleteButton.addEventListener('click', function() {
            listItem.remove(); // Eliminar el elemento al hacer clic en "Eliminar"
        });

        listItem.appendChild(deleteButton); // Añadir el botón de eliminar al <div>
        
        // Generar un color de fondo aleatorio
        const randomColor = `#${Math.floor(Math.random()*16777215).toString(16)}`;
        listItem.style.backgroundColor = randomColor;

        // Generar posición aleatoria dentro de la pantalla
        const screenWidth = window.innerWidth;
        const screenHeight = window.innerHeight;
        const itemWidth = 220; // Ancho del elemento (con el botón de eliminar)
        const itemHeight = 50; // Alto del elemento (ajustar según el contenido)

        const randomX = Math.random() * (screenWidth - itemWidth);
        const randomY = Math.random() * (screenHeight - itemHeight);

        listItem.style.left = `${randomX}px`;
        listItem.style.top = `${randomY}px`;

        document.body.appendChild(listItem); // Añadir el elemento al body

        newTaskInput.value = ''; // Limpiar el input después de agregar la tarea
    }
});
